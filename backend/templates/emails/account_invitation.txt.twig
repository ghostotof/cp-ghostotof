{% extends 'emails/base.txt.twig' %}

{% block body %}
{{ strings.heading }}

{{ strings.intro }}

{{ strings.usernameLabel }} : {{ username }}

{{ strings.cta }} :
{{ setupUrl }}

{{ strings.expiryNote }}

{{ strings.ignoreNote }}
{% endblock %}
