# Identité utilisée par le pipeline GitHub Actions pour déployer (kubeconfig
# statique généré à partir du token de ce ServiceAccount, jamais commité —
# voir k8s/README.md). Remplace le tunnel GitLab Agent for Kubernetes.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: github-actions-deployer
